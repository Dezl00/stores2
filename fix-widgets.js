const fs = require("fs"); let c = fs.readFileSync("src/features/widget-builder/actions.ts", "utf8"); c = c.replace(/where: \{ slug_storeId: \{ slug, storeId \} \}/g, "where: { slug, storeId }"); c = c.replace(/item\.widget\.type/g, "item.widgetId"); c = c.replace(/await db\.widget\.create/g, "await db.widget.create"); c = c.replace(/storeId\s*\n\s*\}/g, "}\n"); fs.writeFileSync("src/features/widget-builder/actions.ts", c);
